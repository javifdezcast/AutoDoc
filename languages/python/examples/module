"""{{ description }}

{%- if attributes %}

Attributes:
{%- for attr in attributes %}
    {{ attr.name }} ({{ attr.type }}): {{ attr.description }}
{%- endfor %}
{%- endif %}
{%- if example %}

Example:
    {{ example }}
{%- endif %}
"""